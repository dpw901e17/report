\usepackage{tikz}
\usetikzlibrary{fit, shadows, arrows, calc}

\tikzset{box/.style = {draw, fill=blue!10, text width=2.3cm, minimum height=3em, drop shadow, align=center}}
\tikzset{arrow/.style={draw, ->, thick, >=stealth, shorten >=1pt}}
